# =======================
# SERVER CONFIG
# =======================
server.port=8443
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=Mypass123
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=tomcat

# Error Handling
server.error.whitelabel.enabled=false
server.error.include-message=always
server.error.path=/error

# =======================
# DATABASE (H2)
# =======================
spring.datasource.url=jdbc:h2:mem:travel_agency;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=none
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
spring.jpa.show-sql=false

# =======================
# SECURITY / JWT
# =======================
application.security.jwt.secret-key=404E635263336A586E3272357538782F413F4428472B4B6250645367566B5970
application.security.jwt.expiration=3600000 # 1 година
application.security.jwt.refresh-token.expiration=604800000

spring.security.enabled=true
logging.level.org.springframework.security=DEBUG

# =======================
# H2 CONSOLE
# =======================
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# =======================
# EMAIL CONFIG
# =======================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=gfotx48@gmail.com
spring.mail.password=wursthnwoqhmkocn
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# =======================
# LOCALIZATION
# =======================
spring.messages.basename=messages
spring.mvc.locale-resolver=accept-header

# =======================
# OAuth2 Google
# =======================

spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/google
spring.security.oauth2.client.registration.google.scope=openid,profile,email
spring.security.oauth2.client.registration.google.client-name=Google

# =======================
# Optional: frontend redirect after login
# =======================
app.oauth2.default-redirect-url=https://localhost:8443/
